<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/econka_prilogenie"
        android:enableOnBackInvokedCallback="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/econka_prilogenie_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.YourVelociraptorFlowers"
        tools:targetApi="31">
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.Login_activity"
            android:label="Login Activity">
        </activity>
        <activity
            android:name=".ui.User_profil"
            android:label="User Profile">
        </activity>
        <activity
            android:name=".ui.Register_activity"
            android:label="Register Activity" />
        <activity
            android:name=".ui.OpisanieActivity"
            android:label="Opisanie Activity" />
    </application>

</manifest>